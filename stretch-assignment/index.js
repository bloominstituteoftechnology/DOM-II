// const blocks = document.querySelector('.blocks');

// const all = blocks.querySelectorAll('div');

// const red = blocks.querySelector('.block.block--red');
// const blue = blocks.querySelector('.block.block--blue');
// const green = blocks.querySelector('.block.block--green');
// const pink = blocks.querySelector('.block.block--pink');
// const gray = blocks.querySelector('.block.block--gray');

// // blocks.style.position = 'relative';

// // red.style.position = 'relative';
// // blue.style.position = 'relative';
// // green.style.position = 'relative';
// // pink.style.position = 'relative';
// // gray.style.position = 'relative';

// red.addEventListener('click', (event) => {

//   red.remove();
//   blocks.prepend(red);

// });

// blue.addEventListener('click', (event) => {

//   blue.remove();
//   blocks.prepend(blue);

// });

// green.addEventListener('click', (event) => {

//   green.remove();
//   blocks.prepend(green);

// });

// pink.addEventListener('click', (event) => {

//   pink.remove();
//   blocks.prepend(pink);

// });

// gray.addEventListener('click', (event) => {

//   gray.remove();
//   blocks.prepend(gray);

// });
