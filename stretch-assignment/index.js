window.onload = function () {
    const blocks = document.querySelectorAll('.block');
    blocks.forEach((item, index) => {
        item.addEventListener('click', (e) => {
            e.stopPropagation();
            let tempColor = blocks[0];
            
        }


}