import './less/index.less'


// Your code goes here!
const mouseOver = document.querySelectorAll('a');

const keyDown = document.querySelector('body');

const wheelTurn = document.querySelector('footer');

const loadTopage = document.querySelector('img');

const focusOntext = document.querySelector('.text-content');

const resizeWindow = document.querySelector('*');

const scroll = document.querySelector('section');

const select = document.querySelector('src');

const onContextMenu = document.querySelector('html');
